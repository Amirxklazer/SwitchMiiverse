name: Switch Homebrew Builder

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkita64:latest
    
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Update System CA Certificates
        run: |
          # Fix potential TLS verification issues that cause 403 errors
          apt-get update -qq && apt-get install -y -qq ca-certificates

      - name: Install Dependencies with Retry
        run: |
          # Retry the package sync up to 5 times in case of transient network errors
          for i in 1 2 3 4 5; do
            echo "Attempt $i: Syncing package databases..."
            dkp-pacman -Sy --noconfirm && break || sleep 10
          done
          # Install the switch development group
          dkp-pacman -S switch-dev --noconfirm

      - name: Write main.c Source Code
        run: |
          cat << 'EOF' > main.c
          #include <stdio.h>
          #include <stdlib.h>
          #include <string.h>
          #include <switch.h>

          int main(int argc, char **argv)
          {
              PadState pad;
              consoleInit(NULL);
              padConfigureInput(1, HidNpadStyleSet_NpadStandard);
              padInitializeDefault(&pad);

              printf("\x1b[16;18mSwitch Homebrew Initialized Successfully!\n");
              printf("\x1b[20;12mPress PLUS (+) on your controller to exit.\n");

              while(appletMainLoop())
              {
                  padUpdate(&pad);
                  u64 kDown = padGetButtonsDown(&pad);
                  if (kDown & HidNpadButton_Plus) break;
                  consoleUpdate(NULL);
              }

              consoleExit(NULL);
              return 0;
          }
          EOF

      - name: Generate Makefile
        run: |
          cat << 'MAKEEOF' > Makefile
          ifeq ($(strip $(DEVKITPRO)),)
          $(error "Please set DEVKITPRO in your environment. export DEVKITPRO=<path to>devkitpro")
          endif

          include $(DEVKITPRO)/libnx/switch_rules

          TARGET      := SwitchMiiverse
          BUILD       := build
          SOURCES     := .
          DATA        := data
          INCLUDES    := .

          # Essential flags for modern libnx compatibility
          ARCH        := -march=armv8-a+crc+crypto -mtune=cortex-a57 -mtp=soft -fPIE
          CFLAGS      := -g -Wall -O2 -ffunction-sections -fdata-sections $(ARCH) $(INCLUDE) -D__SWITCH__
          CXXFLAGS    := $(CFLAGS) -fno-rtti -fno-exceptions
          ASFLAGS     := -g $(ARCH)
          LDFLAGS     := -specs=$(DEVKITPRO)/libnx/switch.specs -g $(ARCH) -Wl,-Map,$(notdir $*.map) -Wl,-z,notext

          LIBS        := -L$(DEVKITPRO)/libnx/lib -L$(DEVKITPRO)/portlibs/switch/lib -lnx

          ifneq ($(BUILD),$(notdir $(CURDIR)))
          export OUTPUT   := $(CURDIR)/$(TARGET)
          export TOPDIR   := $(CURDIR)
          export VPATH    := $(foreach dir,$(SOURCES),$(CURDIR)/$(dir)) \
                              $(foreach dir,$(DATA),$(CURDIR)/$(dir))
          export DEPSDIR  := $(CURDIR)/$(BUILD)

          CFILES      := $(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.c)))
          CPPFILES    := $(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.cpp)))
          SFILES      := $(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.s)))

          export OFILES   := $(CFILES:.c=.o) $(CPPFILES:.cpp=.o) $(SFILES:.s=.o)
          export INCLUDE  := $(foreach dir,$(INCLUDES),-I$(TOPDIR)/$(dir)) \
                              -I$(DEVKITPRO)/libnx/include -I$(DEPSDIR)

          .PHONY: $(BUILD) clean all

          all: $(BUILD)

          $(BUILD):
          	@[ -d $@ ] || mkdir -p $@
          	@$(MAKE) --no-print-directory -C $@ -f $(CURDIR)/Makefile

          clean:
          	@echo clean ...
          	@rm -fr $(BUILD) $(TARGET).elf $(TARGET).nro $(TARGET).nacp

          else
          DEPENDS := $(OFILES:.o=.d)
          $(OUTPUT).nro: $(OUTPUT).elf
          $(OUTPUT).elf: $(OFILES)
          	$(CXX) $(LDFLAGS) $(OFILES) $(LIBS) -o $@
          -include $(DEPENDS)
          endif
          MAKEEOF

      - name: Build
        run: make

      - name: Upload NRO
        uses: actions/upload-artifact@v4
        with:
          name: SwitchMiiverse-NRO
          path: ./*.nro
